## EZ USB Power v0.1.0

### Highlights
- Added modern PyQt desktop UI with live USB power-state refresh.
- Added per-device toggles, search, sorting, and device-type filtering.
- Added on-demand UAC elevation for write actions with explicit success/failure feedback.
- Added `build_windows_release.ps1` to compile and package Windows releases.
- Updated README with screenshot and build instructions.

### Release Assets
- `EZ_USB_Power_v0.1.0_win64.zip`
